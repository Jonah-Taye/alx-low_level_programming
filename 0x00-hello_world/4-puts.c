#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char printText = "\"Programming is like building a multilingual puzzle\n";
	
	puts(printText);
	return (0);
}
