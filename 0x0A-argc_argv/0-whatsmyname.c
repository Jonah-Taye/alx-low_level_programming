#include <stdio.h>

/**
 * main - Prints the name of the program
 * @argc: number of arguments passed
 * @argv: number of arrays passed in the command line
 * Return: Always 0 (Success)
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	(void)argc;

	return (0);
}
